        # NexFit maintenance switch.
        # Include this block as the first lines inside frontend location /.
        if (-f /srv/mykaizenfit/pro/data/maintenance/maintenance.on) {
            return 503;
        }
