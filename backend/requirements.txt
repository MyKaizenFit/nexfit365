# Dev / CI: runtime + test/lint tooling.
# Production image installs only requirements-prod.txt (see Dockerfile).
-r requirements-prod.txt

# Testing
pytest>=7.4.0
pytest-django>=4.12.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
factory-boy>=3.3.0
model-bakery>=1.18.0
freezegun>=1.2.0
coverage>=7.3.0

# Lint / security (local + pre-commit)
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
bandit>=1.7.0
