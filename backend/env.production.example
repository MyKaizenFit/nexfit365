# Configuración de producción para MyKaizenFit Backend
# Este archivo se usa como referencia para configurar las variables en Render

# Django
DEBUG=False
SECRET_KEY=your-super-secret-key-here
ALLOWED_HOSTS=mykaizenfit-backend.onrender.com,localhost,127.0.0.1

# Base de datos (se configura automáticamente en Render)
DATABASE_URL=postgres://username:password@host:port/database

# Redis (se configura automáticamente en Render)
REDIS_URL=redis://username:password@host:port/database

# CORS — production: HTTPS app origins only (no localhost / LAN / raw IP).
# Local compose uses backend/env or docker-compose overrides instead.
CORS_ALLOWED_ORIGINS=https://nexfit365.dpdns.org,https://www.nexfit365.dpdns.org
CORS_ALLOW_CREDENTIALS=True
CSRF_TRUSTED_ORIGINS=https://nexfit365.dpdns.org,https://www.nexfit365.dpdns.org

# Archivos estáticos
STATIC_URL=/static/
STATIC_ROOT=/opt/render/project/src/staticfiles/
MEDIA_URL=/media/
MEDIA_ROOT=/opt/render/project/src/media/

# JWT
JWT_ACCESS_TOKEN_LIFETIME=15
JWT_REFRESH_TOKEN_LIFETIME=7

# Logging
LOG_LEVEL=INFO

# Cache
CACHE_TTL=300

# Rate limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_BURST=10

# Email (opcional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Sentry (opcional)
SENTRY_DSN=























