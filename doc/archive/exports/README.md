# Archived data exports

CSV exports moved out of `backend/` root during audit sanitization (2026-07-24).
Catalog/content only — not end-user PII. Prefer regenerating from DB over committing new dumps.
